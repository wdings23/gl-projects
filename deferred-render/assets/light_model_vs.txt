layout (location = 0) in vec4 position;

uniform mat4 view_matrix;
uniform mat4 projection_matrix;

varying vec4 vPosition;

void main( void )
{
	vPosition = projection_matrix * view_matrix * position;	
	gl_Position = vPosition;
}